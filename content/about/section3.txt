The first thing you should do is make sure you have both the g++ compiler and the Pistache libraries installed on your Linux machine (this also works with WSL on Windows).

Check the included pages to see examples of how to write a page. You can then edit the Pages class in src/pages.h and route your page to a link, like so:
